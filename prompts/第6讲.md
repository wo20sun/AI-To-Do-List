# 一、基本认识

``` mermaid
graph LR
    subgraph 输入端
        Image[图像/音频]
        Text[文本指令]
    end

    subgraph 核心三模块
        Direction1[模块1: 模态编码器]
        Direction2[模块2: 模态接口/对齐器]
        Direction3[模块3: 大语言模型 LLM]
    end

    subgraph 输出端
        Output[文本/图像生成]
    end

    Image --> Direction1
    Direction1 -- "特征提取 (Feature)" --> Direction2
    Direction2 -- "语义对齐 (Embedding)" --> Direction3
    Text --> Direction3
    Direction3 --> Output

    style Direction1 fill:#e1f5fe,stroke:#01579b,stroke-width:2px
    style Direction2 fill:#fff9c4,stroke:#fbc02d,stroke-width:2px
    style Direction3 fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
```

# 二、文本模型

```
import OpenAI from 'openai';

const client = new OpenAI({
  baseURL: 'https://openrouter.ai/api/v1',  //OpenRouter 的 baseURL
  apiKey: '<OPENROUTER_API_KEY>',
});

// First API call with reasoning
const apiResponse = await client.chat.completions.create({
  model: 'google/gemini-3-pro-preview',  //模型名
  messages: [
    {
      role: 'user' as const,
      content: "How many r's are in the word 'strawberry'?",
    },
  ],
  reasoning: { enabled: true } 
});

```

# 三、图片模型


```
import OpenAI from 'openai';

const client = new OpenAI({
  baseURL: 'https://openrouter.ai/api/v1',
  apiKey: '<OPENROUTER_API_KEY>',
});

const apiResponse = await client.chat.completions.create({
  model: 'google/gemini-3-pro-image-preview',
  messages: [
    {
      role: 'user' as const,
      content: 'Generate a beautiful sunset over mountains',
    },
  ],
  modalities: ['image', 'text']
});

const response = apiResponse.choices[0].message;
if (response.images) {
  response.images.forEach((image, index) => {
    const imageUrl = image.image_url.url; // Base64 data URL
    console.log(`Generated image ${index + 1}: ${imageUrl.substring(0, 50)}...`);
  });
}
```

# 五、做一个 AI 视频

## 5.1
```
你是专业的反转、搞笑文案专家，将这段对话中的宝宝发言进行修改，以得到一段程序员喜欢看、和原文一样简短、有趣、让人有转发欲的对话（妈妈说的话原封不动），要求风格是大白话，简单易懂，给出 3 种不同的结果：妈妈说：长大后要不要养妈妈 宝宝说：要 妈妈说：没钱怎么办 宝宝说：去找工作 妈妈说：要是工作也找不到呢 宝宝说：我去捡破烂养妈妈
```


```
这是一段对话文本，为其中的宝宝生成动作描述和镜头语言（剪短一点），如：镜头推进，他咬了口包子，对着镜头哭着说。核心画面是可怜兮兮的边吃包子边哭的回答问题。【对话文本】：妈妈说：长大后要不要养妈妈 宝宝说：要妈妈说：没钱怎么办 宝宝说：接外包搞钱 妈妈说：要是工作也找不到呢 宝宝说：去做自媒体带货养你
```

## 5.2
```
基于提供的图片，生成一张站立的哭泣小狗，手里拿着包子，眼角流着泪珠，真实风格
```
